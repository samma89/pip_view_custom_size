name: pip_view_custom_size
description: Widget to allow the presentation of a widget below a floating one with custom size.
version: 0.10.0
homepage: https://github.com/samma89/pip_view_custom_size

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
